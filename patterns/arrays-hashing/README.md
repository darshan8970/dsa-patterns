# Arrays & Hashing

Frequency counting, hashmaps for O(1) lookup, and prefix-sum tricks. The pattern to reach for whenever you catch yourself writing a nested loop to check "have I seen this before?"

## Problems solved here

| # | Problem | Difficulty | Notes |
|---|---|---|---|
| 1 | [Two Sum](problems/0001-two-sum) | Easy | Hashmap complement lookup, one pass |

*Add a row here (linking to its `problems/NNNN-slug/` folder) as you solve each problem.*

## Key ideas / gotchas

- _Add pattern-specific notes as you build intuition -- e.g. common off-by-one traps,_
  _when this pattern is a red herring, related patterns it's often confused with._
